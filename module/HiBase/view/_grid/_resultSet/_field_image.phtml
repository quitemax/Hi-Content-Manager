<td class="text <?php if ($this->sort):?>sort<?php endif;?>" >
  <?php echo $this->showImage(
          $this->value,
          array(
              'width'    => 100,
              'height'   => 100,
              'alt'      => 'image',
              'cache'    => $this->cache,
          )
      );?>
</td>
